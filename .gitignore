# Node test toolchain (vitest/happy-dom in editor/).
# package-lock.json IS committed on purpose so `npm ci` reproduces the env.
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
*.swp

./openspec*
openspec*
.opencode*